# Copy to .env.local and set when using a phone tunnel (cloudflared / ngrok).
# Must match the HTTPS URL you open on the phone (Vite proxies /api and /ws).
# VITE_PUBLIC_ORIGIN=https://YOUR-SUBDOMAIN.trycloudflare.com
